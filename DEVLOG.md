# JELLYFISH HUNTER 開発の記録

仕様書 1枚から遊べる状態までを、2日・約30コミットで作りました。
何をどういう理由で変えてきたかを、時系列でまとめたものです。
個々の数値や調整値は [README](README.md) にまとまっています。

---

## 0. 出発点

手元にあったのは次の2つだけでした。

- **仕様書**（`jellyfish-hunter-spec.md`）— 海中が舞台の3Dオンレール弾幕シューティング。
  なぞってロックオン、倒した敵が味方になる、ボスは部位ごとに壊せる、1周クリアで解放要素。
- **3Dモデル6体**（自機・雑魚3種・中ボス・ボス）— 子どものイラストを画像生成で起こし、
  Meshy で3D化したもの。

ここから、GitHub Pages に置くだけで遊べる形を目指しました。

---

## 1. 土台づくり

### 1ファイル構成にした

HTML・CSS・JavaScript をすべて `index.html` に同梱しています。ビルドもサーバも要らず、
GitHub Pages に置けばそのまま動き、ファイルを1つ開けば全体を追えます。

### three.js をリポジトリに入れた

CDN を読みに行く作りだと、ネットワークが不調なときに動かず、将来 CDN が消えれば
遊べなくなります。three.js と GLTFLoader などをリポジトリに同梱し、
importmap で解決する形にしました。オフラインでも動きます。

### モデルを `models/` に整理した

バラバラだった `.glb` をすべて `models/` へ移動。読み込みは進捗つきの一括ローダーに
まとめ、タイトル画面にロード率を出すようにしました。

---

## 2. 最初のテストプレイ — 致命的な不具合3件

遊んでもらったところ、3つの問題が同時に報告されました。

- ロックオンができない（＝味方も増えない）
- ボスにだけはロックオンできる
- 自機に当たり判定がなく、無敵になっている

一見バラバラですが、**原因は2つ**でした。

ひとつは、時間を減らしていくカウンタが 0 を通り越して**わずかな負の値**で止まっていたこと。
コード側はそれを「0 かどうか」ではなく真偽値として見ていたため、`-0.003` のような値が
「まだ残っている」と判定され続けていました。結果、雑魚は永久に「出現中＝ロックオン不可」、
自機は永久に「無敵中」になっていました。減算のたびに 0 で止め、判定も明示的な大小比較に
統一して、同じ書き方が他にないか全体を確認しました。

もうひとつは、自機の当たり判定に**常に原点を指す座標**を使っていたこと。親子関係のある
オブジェクトのローカル座標を読んでいたためで、ワールド座標を見るように直しました。

さらに、画面を覆っていた UI のレイヤーがクリックを全部吸い取っていたことも判明し、
これも合わせて直しています。

---

## 3. 難易度を3段階にした

「NORMAL のボスが一瞬で溶ける」「HARD の歯ごたえが足りない」という感想を受けて、
難易度そのものを作り直しました。

- **NORMAL / HARD / NIGHTMARE** の3段階に。NIGHTMARE は HARD クリアで解放。
- 敵の数・耐久・弾速・発射間隔・弾幕の広がりを**1つのテーブルに集約**。
  数値を書き換えるだけでバランス調整でき、ランクの追加も容易です。
- 敵を増やすとき、**横に広げず奥行きに重ねて波状**にしました。1グループが通り過ぎる
  時間が伸びるので、結果として手が空く時間も埋まります。
- それでも群れが途切れる瞬間があったので、**自動で増援を差し込む仕組み**を追加。
- ボスは HARD 以上で**攻撃トラックが2本**になり、通常攻撃と並行して追加の弾幕を撃ちます。

最後に、HARD 以上では**敵の湧く位置そのものを横に広げました**。陣形ごとに乱数の横ずれを
与えるので、中央から来ることもあれば画面の端から来ることもあり、狙う方向が固定されません。

---

## 4. 遊び心地の往復

テストプレイのたびに、細かい手触りを詰めていきました。

- **ロックオンレーザーの威力** — 「撃っても雑魚が死なない」の原因は描画ではなく、
  単純に威力が雑魚の耐久を下回っていたこと。難易度ごとの撃破ラインを表にして、
  どこまで1発で倒せるかを選んでもらう形にしました。
- **敵弾の色を赤系に統一** — 弾が増えると何が危険なのか分からなくなるため。
  自機の黄・味方の水色はそのままにして、「赤＝当たると痛い」を徹底しました。
- **無敵時間** — 仕様書の想定より大幅に短くしました。長いと被弾がうやむやになるためです。
- **味方の消費順** — 被弾でもボムでも**古い味方から**失う形に。新しく仲間にしたばかりの
  味方が手元に残るので、獲得の手応えが消えません。
- **ボムのコスト**と**ボスの触手攻撃の重さ**を引き上げ、大技として成立させました。

---

## 5. 画面まわり

- **ウィンドウサイズへの追従** — ウィンドウの縦横比が変わっても見切れないように、
  横に狭いときは画角を自動で広げるようにしました。全画面切替・ズーム・ブラウザUIの
  増減など、理由を問わずキャンバスの実寸に追従します。
- **全画面表示** — 当初は F キーでしたが、移動の D の隣で誤爆するため **Enter キー**に
  変更。Enter はフォーカスの残ったボタンを押してしまう副作用があるので、それも抑止して
  います。
- **あそびかた画面** — 内容が画面に収まらず、しかも上にスクロールできない状態でした。
  中央揃えの指定が原因だったので、スクロールできる形に組み直しました。
- **タイトル画面の読みやすさ** — 下部の暗い帯をスクロールしても流れない作りに変えて、
  低いウィンドウでも文字が絵に埋もれないようにしました。

---

## 6. 見た目を仕上げる

### ボスクラゲに動きをつけた

途中でボーン入りのモデルを差し替えてもらい、**触手2本が独立して動く**ようにしました。
待機中は根元から先端へ波が伝わるようにゆらゆら揺れ、攻撃のたびに全身が躍動します。
ロックオンの判定位置も骨に紐づけたので、触手が揺れれば枠も一緒に動きます。
骨のないモデルに差し替えても動くよう、従来方式へのフォールバックも残してあります。

### ボスを画面いっぱいに

「小さく感じる」との指摘を受けて、大きさを一気に引き上げました。見た目だけ変えると
判定がずれるので、当たり判定・発光・触手の太さまで**同じ倍率で連動**させ、
定数1つで全部が揃って変わるようにしています。あわせて構える距離と上下の揺れを調整し、
巨体でもロックオンの枠が画面から出ないようにしました。

### 海底に飾りを置いた

サンゴ（赤・青）・海藻・遺跡・貝殻・沈没船の6種を海底に散らしました。
種類ごとに1つのまとまりとして描くので、100体以上置いても描画の負担はほとんど増えません。
プレイヤーが通り過ぎた飾りは奥へ巻き戻して使い回すため、ステージのどこでも景色が途切れません。

なお、もらった6ファイルは合わせて約50MBありました。高解像度のテクスチャが3種類ずつ
入っていたので、実際に必要な色のテクスチャだけを残して再圧縮し、**1/25以下**に。
近距離とゲーム中の距離の両方で並べて比較し、見た目が変わらないことを確認しています。
形状のデータには一切手を加えていません。

### タイトルとエンディングを1枚絵に

用意してもらったイラストに差し替えました。タイトルは絵の上部にロゴが入っているので
上端を基準に敷き、下側にだけ暗い帯を重ねてメニューを読ませています。
**メニュー以外をクリックするとメニューが消えて絵の全体を鑑賞できる**モードも付けました。

### レーザーを太くした

一斉発射の手応えを出すため、レーザーの描き方を作り直しました。もともと使っていた線の
描画は太さを指定できない仕様で、離れると髪の毛のようにしか見えなかったためです。
軌跡に沿って帯を張り、芯を白く光らせ、先端ほど太く明るくなるようにしました。
着弾後もわずかに弧が残るので、10体同時に撃つと弧が何本も同時に空へ残ります。

---

## 7. 音

当初は外部素材なしで自己完結させるため、効果音もBGMもコードで合成していました。
その後、用意してもらった**ステージBGM・ボス戦BGM・勝利のファンファーレ**に差し替えています。

曲全体をメモリに展開せずストリーム再生する作りなので起動は軽いままで、タイトル表示の
タイミングで先読みを始めます。曲の切り替えではフェードアウトし、ポーズは再生位置を
保ったまま止まり、リトライでは頭出しされます。ファンファーレだけはループせず、
エンディングの1枚絵と同時に1回だけ流れます。ゲームオーバーにも専用の曲を1回だけ流します。
効果音は引き続き合成音です。

---

## 8. 遊ぶのは小学1年生

途中から「プレイする子どもは小学1年生」という前提が共有され、表現を全面的に見直しました。

- あそびかたの「敵」「味方」を**すべてひらがな**に。見出しだけは漢字にふりがなを添えました。
- 説明文を、トレードオフや難易度の話を省いた**やさしい言葉**に書き直し。
- ゲーム中の「僚機」という言葉を**「味方」**に統一。
- ボムの説明を、実際の効果がそのまま伝わる文に変更。
- 全画面キーを誤爆しにくい Enter に変更（前述）。

一方で、あそびかたを読むのは大人です。そこで**見出しの右のボタンで、子ども向けと
大人向けの2つの表示を切り替えられる**ようにしました。大人向けには、ロックオン中は
味方の攻撃が止まるという駆け引きや、ボムを使うか温存するかの判断まで書いてあります。

---

## 9. つまずいた点

正直に残しておきます。

**GitHub の Web エディタでバイナリを触ってしまった。**
`.glb` の移動を「鉛筆アイコンで編集 → ファイル名を変更」で案内したところ、Web エディタが
バイナリを扱えず、6つのモデルが2バイトの壊れたファイルになりました。ハッシュ比較で
気づき、無事なファイルを渡し直して「Upload files」で復旧しています。
**バイナリに Web エディタは使わない**、が教訓です。

**push が 403 で通らなかった。**
GitHub App の対象リポジトリが「選択したリポジトリのみ」になっており、公開リポジトリは
読み取り専用として扱われていました。読めるのに書けない、という症状の原因がこれです。
対象を全リポジトリに変更してもらって解決しました。

**誤った説明をした。**
権限設定の手順と、レーザー威力の見積もりで事実と違う説明をし、どちらも訂正しました。

---

## 技術的にこだわった点

- **数値を1か所に集約した。** 弾速・威力・耐久・難易度倍率・ボスの大きさなど、調整しうる
  値はすべて先頭の定数テーブルに集めてあります。テストプレイの指摘に、探し回らずに
  1行の書き換えで応えられる状態を保ちました。
- **タイマーをゲーム内時間で動かした。** 遅延処理にブラウザのタイマーを使うと、ポーズ中も
  時間が進み、やり直しても前回の予約が残って撃ってきます。自前のスケジューラに置き換えました。
- **モデルの差し替えに強くした。** 大きさはモデルの実寸から自動計算し、ボスの骨は名前で
  引きます。差し替えても見た目の大きさは変わらず、骨がなければ従来方式に自動で戻ります。
- **調整用の隠しコマンドを用意した。** タイトル画面で決まった並びのキーを押すと、解放状態を
  消して初回起動の状態に戻せます。解放前の見え方を確認したいときのためのもので、
  遊んでいて偶然発動しない条件を付けてあります。
- **毎回ブラウザで検証した。** ヘッドレスブラウザで実際に起動し、ロックオンから味方化、
  ボス戦、エンディングまで通して動かし、スクリーンショットとコンソールエラーの有無を
  確認してから push しています。

---

## できあがったもの

- 海中を進む3Dオンレール弾幕シューティング、1ステージ＋ボス。
- なぞってロックオン → 離して一斉発射。倒した敵が味方になり、味方がライフを兼ねます。
- ボスは4部位を個別に壊せ、触手は独立して動きます。
- 難易度3段階、1周クリアで武器セットアップと HARD、HARD クリアで NIGHTMARE が解放。
- タイトル／エンディングは描き下ろしの1枚絵。BGMはステージ・ボス戦・勝利・ゲームオーバーの4曲。
- あそびかたは子ども向けと大人向けを1クリックで切り替えられます。
- ビルド不要の1ファイル構成。GitHub Pages に置くだけで動き、オフラインでも遊べます。

遊ぶ：https://minaasa373-wq.github.io/jellyfish-hunter/
